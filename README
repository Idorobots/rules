A basic implementation of the Rete inference algorithm with support for reduction nodes, filtration nodes, forward- & backward-chaining.
Check src/examples.scm for usage examples. See src/benchmark.scm and benchmark/ directory for some half-assed benchmarks.
